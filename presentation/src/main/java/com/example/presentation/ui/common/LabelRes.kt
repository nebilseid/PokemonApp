package com.example.presentation.ui.common

import com.example.presentation.R

enum class LabelRes(val resId: Int) {
    Name(R.string.label_name),
    Height(R.string.label_height),
    Weight(R.string.label_weight),
    Types(R.string.label_types),
    Abilities(R.string.label_abilities)
}
